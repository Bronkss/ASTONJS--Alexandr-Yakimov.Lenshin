// Проверяем commit-msg

// Проверяем commit-msg 2 попытка.

// Проверяем commit-msg hook (3 попытка)

// Проверяем commit-msg hook (4 попытка)

// Проверяем commit-msg hook (5 попытка)

// Успех!! Теперь скрипт корректно выполняется по заданному паттерну. 